name: Security checks

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  security:
    permissions:
      contents: read
      pull-requests: write
    uses: shuymn/github-actions/.github/workflows/renovate.yml@a62adc7529abc4770f3cb99fd5cf98be797c5795
